# Active Original ↔ Nex Parity Audit Index

## Authority

This file is the moving execution index under the fixed contract in `ORIGINAL_NEX_DELIVERY_KNOWLEDGE_GRAPH.md`.

It records the current real audit position. Historical matrix counts, implementation volume and green CI do not override this index. Exact moving Head and workflow conclusions are read from Draft PR #11 and GitHub Checks rather than copied here.

## Current repository state

- Branch: `feat/m8-profile-workflow`.
- PR: #11, Draft.
- Active candidate: none.
- Failed candidate: `M8-OWNER-QC-1`.
- Provisional total progress: 47% (unrounded 46.7%; confidence band 42%–50%).
- Merge/release/candidate generation: prohibited.
- Progress authority: `docs/PROJECT_PROGRESS_MODEL.md`.
- Active journey: Order 1 at 35%.
- Current implementation registers one background owner for real browser Action writes and composes the repository resolver, global/per-tab coordinator, exact renderer, runtime localization and Inspect overlay.
- The unified Original-observable projection now covers Direct/System/Fixed, exact one-level Switch results into Direct/Fixed, the exact two-level nested Switch 01I subset, exact immediate Virtual results into Direct/Fixed proxy/bypass and the exact attached Rule List 01H subset.
- Startup establishes a browser-level System/Direct Action baseline, serializes profile-workflow commands through activation follow-up, repairs missing proxy runtime from the saved startup route and refreshes all tabs after activation or recovery. Top-level `webNavigation.onCommitted` remains a final-URL coordinator input.
- Real Chromium and Firefox Action acceptance covers every represented shape. Native Chromium Inspect verifies set/clear/base restoration/isolation.
- The permanent read-only `Original Toolbar Evidence` workflow now provides one parameterized original-package harness rather than one workflow per trace.
- Remaining nested Switch shapes, nested Virtual, remaining attached Rule List shapes, PAC, temporary-rule/external-control traces, renderer fallback/pixels and owner acceptance remain open.

## Active delivery sequence

| Order | Journey                                                  | Contract/order                       | Evidence                                                                                    | Current state                                                                                                                                                                                                                                                                                                     | Next gate                                                                                                                     |
| ----: | -------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
|     1 | Installation / startup / toolbar                         | `DELIVERY_ORDER_01_TOOLBAR_STATE.md` | Original source/package/runtime evidence plus permanent Nex Chromium and Firefox Action E2E | Single-writer runtime, global/per-tab Action state, Direct/System/Fixed, exact one-level Switch, exact 01I two-level nested Switch, immediate Virtual, exact 01H attached Rule List, internal fallback and Inspect are automation-verified in both targets. Remaining result families and owner PASS remain open. | Capture nested Virtual next, then remaining Rule List/PAC/temp/external/fallback states and run focused owner acceptance      |
|     2 | Real original export → direct Nex use                    | pending                              | pending real-export corpus                                                                  | `FAILED`; owner’s real original export did not become directly usable. `KG-IMPORT-COLOR-001` also records unresolved original `#RGB` normalization.                                                                                                                                                               | Capture exact original backup semantics and first real sanitized owner export; compare fields/references/startup/Quick Switch |
|     3 | Popup                                                    | pending                              | original Popup source and basic Chromium/Firefox runtime surfaces captured                  | broad mismatch reported; complete state hierarchy remains open                                                                                                                                                                                                                                                    | Capture original dimensions, hierarchy and all states; compare independently with Nex                                         |
|     4 | Options / Apply / Discard                                | pending                              | source notes and basic installed Chromium/Firefox Options surfaces                          | broad mismatch reported                                                                                                                                                                                                                                                                                           | Page/control/dialog/help-text inventory before code changes                                                                   |
|     5 | Complete profile journeys                                | pending                              | historical implementation inventory plus current narrow Toolbar slices                      | untrusted pending complete journey re-audit                                                                                                                                                                                                                                                                       | Fixed → Switch → PAC → Virtual → Rule List complete journey maps                                                              |
|     6 | Export / restart / rollback / ownership / authentication | pending                              | partial implementation/tests exist                                                          | not owner-complete                                                                                                                                                                                                                                                                                                | Revalidate against original-facing behavior and real imported data                                                            |
|     7 | Localization / density / visual alignment                | pending                              | generated Nex screenshots are not sufficient original comparison evidence                   | broad mismatch reported                                                                                                                                                                                                                                                                                           | Full original runtime captures and side-by-side owner order                                                                   |

## Order 1 checkpoint

### Confirmed original contract

- original manifest supplies the original Action assets, Popup, localized loading title, shortcut and tab-aware permission;
- original toolbar state is per-tab and URL-result-aware;
- one-color and two-color icons are generated dynamically;
- exact renderer geometry is source-captured;
- profile changes invalidate icon state and refresh tabs;
- Direct, Switch/PAC results, Virtual resolution, attached Rule Lists, temporary rules, Inspect and external proxy state affect visible toolbar state;
- optional result-profile Badge text is an original setting and is truncated to four JavaScript string code units;
- official Chromium and Firefox v3.5.0 packages differ in Popup entry, background declaration, authentication permission and minimum Firefox version.

### Permanent parameterized original evidence harness

The read-only `.github/workflows/original-toolbar-evidence.yml` workflow:

- downloads and SHA-256 verifies the official v3.5.0 Chromium package;
- accepts one scenario ID, comma-separated IDs or `all`;
- uses an isolated browser profile for every scenario;
- creates and applies profiles through the original runtime APIs;
- captures original runtime state and `_actionForUrl` results into an auditable JSON Artifact;
- cannot commit or push because repository permissions are read-only.

The scenario registry currently contains `nested-switch`. Additional result families must extend the same registry and runner rather than create another per-trace workflow.

### Captured original runtime

Chromium baseline evidence is durable in `AUDIT_EVIDENCE_01C_ORIGINAL_CHROMIUM_RUNTIME.md`.

Switch-result evidence is durable in `AUDIT_EVIDENCE_01F_ORIGINAL_SWITCH_RESULTS.md`:

- matched/default Switch → Direct titles and `Dire` Badge;
- Direct result color plus current Switch color;
- matched Switch → Fixed multiline details;
- original rejection of Switch → System.

Virtual-result evidence is durable in `AUDIT_EVIDENCE_01G_ORIGINAL_VIRTUAL_RESULTS.md`:

- exact immediate Virtual → Direct `[[Direct]]`, Direct details and `Dire` Badge;
- exact Virtual → Fixed proxy and bypass details;
- target-derived Badge and color inputs;
- suppressed Virtual default transition line.

Attached Rule List evidence is durable in `AUDIT_EVIDENCE_01H_ORIGINAL_ATTACHED_RULE_LIST_RESULTS.md`:

- matched AutoProxy line → Fixed;
- no match → default Direct;
- matched AutoProxy line → Direct;
- no match → default Fixed;
- exact `(RL) ` and `(default)` detail behavior, hidden implementation profile, Badge and color inputs.

Nested Switch evidence is durable in `AUDIT_EVIDENCE_01I_ORIGINAL_NESTED_SWITCH_RESULTS.md`:

- outer matched edge → inner matched edge → Fixed proxy;
- outer matched edge → inner default → Direct;
- outer default → Direct;
- every selected nested edge appears in order;
- final result controls result name/Badge/outer color while the applied outer Switch retains the Action inner/current color.

Firefox baseline evidence is durable in `AUDIT_EVIDENCE_01D_ORIGINAL_FIREFOX_RUNTIME.md` and records initial/System, Direct, second-tab, inactive-tab and internal-page title/Badge/Popup state plus installed Options and Popup surfaces.

The following original states remain `UNKNOWN` in one or both targets:

- PAC result state;
- nested Switch shapes outside 01I, including outer default → inner and deeper chains;
- nested Virtual, Virtual-to-Switch/Rule List/PAC;
- attached Rule List shapes outside 01H, including exclusive, multi-line, bypass, fallthrough and chains;
- temporary-rule state;
- Inspect original runtime state;
- external-controller transition details;
- headed toolbar pixels where source/package evidence is insufficient.

### Current Nex runtime and remaining gap

Confirmed real runtime:

- the background constructs the real Action/i18n/tabs/webNavigation/OffscreenCanvas boundary and registers one Action writer;
- the repository-backed resolver feeds one Original-observable projection rather than exposing internal GraphTrace wording;
- the projection covers Direct, System, Fixed proxy/bypass, exact one-level Switch → Direct/Fixed, the exact 01I two-level nested Switch subset, exact immediate Virtual → Direct/Fixed proxy/bypass and the exact 01H attached Rule List subset;
- the race-safe coordinator owns the global baseline, tab creation, URL updates, activation, all-tab refresh, stale-result suppression and cache invalidation; top-level `webNavigation.onCommitted` supplies the committed URL when Firefox tab events are incomplete;
- clean installations proactively initialize to System without opening Popup or Options; command serialization prevents half-initialized concurrent reads, and missing proxy runtime is repaired from the saved startup route;
- Inspect is an overlay input to the same executor rather than a competing title/Badge writer;
- permanent Chromium and Firefox Action E2E read real tab IDs and verify all represented result families, including shared profile-trace scenario execution for nested Switch;
- native Chromium Inspect captures set/clear/base restoration/isolation;
- cross-document revision archives are filtered by current `documentId` before current-document validation.

Remaining gap:

- nested Switch/profile chains beyond 01I, nested Virtual and Virtual-to-Switch/Rule List/PAC, attached Rule Lists beyond 01H, PAC, temporary-rule and external-control states still lack complete original result traces; Switch → System is original-invalid;
- forced renderer-fallback evidence and headed toolbar pixels remain open where browser-readable Action state is insufficient;
- no toolbar journey is owner-complete until focused repository-owner acceptance passes.

### Source-certain implementation

- exact Ω geometry, original static assets, one/two-color state and five-size OffscreenCanvas renderer;
- original title/detail localization, result-Badge preference and four-code-unit truncation;
- target-specific manifest Action contract, exact required `webNavigation` permission guard and isolated browser Action adapter;
- pure internal decision → Original-observable projection → localization → renderer → executor path;
- strict evidence-bounded subprojectors for attached Rule List 01H and nested Switch 01I;
- repository-backed Direct/System/Fixed resolver plus exact one-level Switch and immediate Virtual reconstruction;
- race-safe per-tab coordinator with creation/update/activation coverage, top-level navigation-commit refresh and background runtime manager;
- single-writer Inspect overlay;
- proactive System initialization, serialized workflow commands and missing-runtime recovery;
- permanent Chromium and Firefox full/Toolbar/attached-Rule-List/profile-trace E2E;
- native Inspect Action set/clear/base restoration/isolation;
- regression coverage for cross-document revision history isolation.

These close implementation and automation slices only. They do not close `KG-ICON-001` or authorize a candidate.

### Current verified engineering checkpoint

The current branch passes permanent CI, original Toolbar evidence, Chromium full/Toolbar/attached/profile-trace E2E, Firefox full/Toolbar/attached/profile-trace E2E, native Chromium Inspect, Parity Documentation and Visual Evidence.

Exact moving Head and run IDs are intentionally read from PR #11 and GitHub Checks. The checkpoint remains slice-level evidence. Progress stays 47%, Order 1 stays 35%, and no candidate is authorized.

### Evidence correction and official packages

Artifact `original-zeroomega-ui-evidence-v3.5.0`, ID `8625759489`, SHA-256 `8403e963325a5d4fcac10fd2f3c8dac246cb720afb24f322c827d5cf8ebfdd19`, contains source/locale files and is not an installed runtime screenshot set.

Exact official v3.5.0 packages are captured:

- Chromium ZIP SHA-256 `4fc320af32461a7efc7d6b7a700100522af29afa6bd1270aef193f9121b160ce`;
- Firefox ZIP SHA-256 `666ce976022ed379601b896f9138ada82a29aabfe0154bf2bf1ae779e3319cda`;
- both ZIP integrity tests passed;
- package evidence: `AUDIT_EVIDENCE_01_ORIGINAL_RELEASE.md`;
- Chromium runtime evidence: `AUDIT_EVIDENCE_01C_ORIGINAL_CHROMIUM_RUNTIME.md`;
- Firefox runtime evidence: `AUDIT_EVIDENCE_01D_ORIGINAL_FIREFOX_RUNTIME.md`;
- Switch-result evidence: `AUDIT_EVIDENCE_01F_ORIGINAL_SWITCH_RESULTS.md`;
- Virtual-result evidence: `AUDIT_EVIDENCE_01G_ORIGINAL_VIRTUAL_RESULTS.md`;
- attached Rule List evidence: `AUDIT_EVIDENCE_01H_ORIGINAL_ATTACHED_RULE_LIST_RESULTS.md`;
- nested Switch evidence: `AUDIT_EVIDENCE_01I_ORIGINAL_NESTED_SWITCH_RESULTS.md`.

Permanent Nex Firefox E2E navigates extension pages through WebDriver BiDi to avoid Marionette rejection of direct `moz-extension://` navigation on newer Firefox runners.

## Active blockers

- `KG-ICON-001` — active Order 1; `FAILED` as a complete journey.
- `KG-IMPORT-001` — queued Order 2; `FAILED`.
- `KG-IMPORT-COLOR-001` — queued Order 2; open migration blocker.
- `KG-UI-001` — project-wide; `FAILED`.
- `KG-EXTRA-001` — project-wide; `FAILED`.
- `KG-INVENTION-001` — project-wide; `FAILED`.
- `KG-FLOW-001` — project-wide; `FAILED`.
- `KG-GOV-001` — completion governance corrected, but final acceptance remains open.

## Execution rule

For each order:

1. capture original source and real runtime evidence;
2. capture current Nex independently;
3. publish the owner-readable Original ↔ Nex mapping;
4. identify missing, broken, extra and invented behavior;
5. implement only against the mapped contract;
6. verify real Chromium, Firefox and real original data where applicable;
7. require repository-owner `PASS`.

No order may be closed from synthetic fixtures, component screenshots, internal state tests or self-authored Nex behavior alone.
