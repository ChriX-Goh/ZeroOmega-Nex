# MIG-01 Existing Asset and Gap Inventory

> Current-state overlay — 2026-08-08: the detailed body below is a historical inventory snapshot from the MIG-01.5 transition and must not be read as current status where it says Corpus C/D, MIG-01.6, or MIG-01.7 are still open. Current authoritative state is: Corpus A complete; Corpus C/D complete; large original-runtime capacity complete; MIG-01.6 failure preservation complete; repository-controlled MIG-01.7 semantic/secret evidence complete; MIG-01.8A owner intake ready; MIG-01.8B real-importer safe preflight ready; MIG-01.8C owner-handoff failure surfaces hardened; MIG-01.8D checkout containment hardened for sanitized owner candidates and preflight report output; Corpus B itself remains absent and is the remaining positive-corpus blocker. Product progress remains `45.15%` (reported `45%`), owner retest remains prohibited, PR #11 remains Draft, and release remains `NO-GO`. See `MIG_01_CORPUS_B_INTAKE.md` and `SESSION_18_MIG_01_8D_KNOWLEDGE_GRAPH.md` for the current handoff.

## 1. Scope

This inventory tracks the importer, original and synthetic corpus, acceptance transaction,
activation, packaged-browser evidence, semantic export, and remaining migration debt.

Evidence levels remain distinct:

1. original-runtime-produced data;
2. synthetic schema fixtures;
3. background/package integration tests;
4. packaged real-browser journeys.

A lower-level pass does not close the parent migration journey.

## 2. Current pipeline

```text
File or online text
  -> importZeroOmegaBackup
  -> compatibility report + inactive candidate + secret materials
  -> user review
  -> accept-import
     -> secret snapshot/write
     -> Draft replacement
     -> rollback Draft/secrets on acceptance failure
  -> apply
     -> validate / compile / install / confirm
     -> preserve or restore prior Applied/runtime state on failure
  -> active browser state
  -> browser restart recovery
  -> exportZeroOmegaBackup
  -> schemaVersion:2 backup
```

The ordinary import UI still composes `accept-import` and `apply`; it is not one background command.

## 3. Corpus state

### Corpus A — official/default: dual-browser packaged journey complete

`fixtures/zeroomega-v2/original-default-v3.5.0.bak`

- generated by the pinned official ZeroOmega v3.5.0 runtime/package;
- provenance recorded in `original-default-v3.5.0.provenance.json`;
- contains schema-v2 settings, Fixed `proxy`, Switch `auto switch`, bypass, startup and Quick Switch;
- uses safe synthetic endpoints while preserving original-runtime serialization.

Corpus A is now covered by:

- importer/exporter unit semantic round trips;
- `apps/extension/src/lib/original-default-migration.test.ts` transaction/failure oracle;
- packaged Chromium import, mapping, PAC activation, real routing, restart, restoration and export;
- packaged Firefox import, mapping, PAC activation, real routing, restart, restoration and export.

The original backup has Quick Switch disabled with an empty route list. Both browser journeys keep
that contract, temporarily activate `auto switch` only through public workflow commands for route
testing, and restore the imported settings before semantic export.

Firefox closure evidence:

- functional fix: `b7e461a5261bf3ab26e7a2f2714bdb0dee81a80d`;
- clean exact evidence checkpoint: `eda9999d888494879aabdc10e940e879933fc460`;
- Browser E2E run `31139057915`: success;
- Firefox job `92744832113`: success;
- CI run `31139057966` and the five other permanent PR workflows: success.

The real Firefox failure was an E2E locale mismatch: the Firefox E2E build is intentionally `zh-TW`
while the new migration script searched `zh-CN` labels. The fix changed test locale/labels only; it
did not alter importer, PAC, route, Popup or Options product behavior.

### Corpus B — owner representative: blocked on real input

Still missing a sanitized export representative of the owner's daily configuration. No repository
evidence currently supplies this data. Do not fabricate it or substitute a synthetic fixture.

### Corpus C — nested semantics: missing original-runtime-produced fixture

Required coverage includes nested Switch, Virtual, attached Rule List, default-result and reference
behavior. Existing synthetic fixtures help with isolated semantics but do not satisfy the contract.

### Corpus D — PAC and operational metadata: missing original-runtime-produced fixture

Required coverage includes PAC URL/body, update/cache state, bypass, supported proxy-auth metadata,
Unicode, valid duplicate names and safe unknown fields. Existing synthetic fixtures remain lower-level
evidence only.

### Negative corpus

Synthetic fixtures already cover many malformed and hostile boundaries, including invalid encoding,
invalid JSON, missing references, cycles, excessive nesting, oversized input, unsupported shapes,
unsafe local PAC and secret-like metadata. Packaged failure injection and the complete two-browser
failure-preservation matrix remain open.

## 4. Importer and semantic assets

`packages/legacy-zeroomega` provides bounded input handling, stable IDs, profile/condition mapping,
reference resolution, cycle checks, capability analysis, secret extraction, inactive candidates,
deterministic reports, safe opaque metadata preservation and explicit downgrade/rejection issues.

Remaining field-classification debt:

- classify each relevant source field as exact, target-dependent, downgraded, unsupported, rejected
  or safely opaque-preserved;
- verify alias identity/color/order on broader original data;
- verify Virtual and target-dependent AutoDetect behavior;
- decide safe opaque preservation versus behavior-changing rejection;
- reconcile operational settings with the no-global-listener architecture.

## 5. Acceptance and Apply transaction state

`acceptProfileWorkflowImport` snapshots affected secrets, writes secret material, replaces Draft and
restores prior Draft/secrets on acceptance failure.

The background official-backup oracle proves:

- analysis -> acceptance -> Apply -> semantic export -> re-import;
- previous active state remains confirmed when browser activation is forced to fail;
- accepted imported Draft remains available after Apply failure;
- secret store is not unexpectedly altered by failed Apply.

Remaining transaction debt:

- decide final wording for “imported but not activated” after Apply failure;
- extend failure injection to packaged browsers and broader real corpus;
- complete the two-browser migration failure matrix.

## 6. Packaged browser state

### Chromium

Corpus A passes ordinary Options import, compatibility review, profile/settings mapping, temporary
public-workflow activation, extension-controlled PAC, real Direct/Proxy decisions, persistent restart,
restoration of original settings and schema-v2 semantic export. Existing Chromium regressions remain
green in the same Browser E2E job.

### Firefox

Corpus A now passes the equivalent packaged Firefox journey in
`scripts/e2e-firefox-original-migration.mjs`, executed before the existing Firefox regressions by
`test:e2e:firefox`.

The clean evidence checkpoint `eda9999d888494879aabdc10e940e879933fc460` has Browser E2E,
CI, Parity Documentation, Milestone 8 Visual Evidence, Original Nex UI Evidence and Original Toolbar
Evidence green. Firefox Toolbar/restart/Rule List/trace/external-control/fallback regressions also pass
on that Head.

This closes the official/default dual-browser slice. It does not prove broader Corpus B-D migration
or packaged failure recovery.

## 7. Export state

`exportZeroOmegaBackup` maps major profile families, operational settings, Quick Switch, startup,
PAC, Rule Lists, bypass, conditions and safe opaque metadata.

Current evidence proves:

- synthetic representative import/export/import equality;
- official default candidate-level import/export/import equality;
- official default Chromium semantic export after packaged migration/restart;
- official default Firefox semantic export after packaged migration/restart;
- omission of credentials, sensitive headers and secret references in the covered journeys.

Remaining field-matrix debt includes credentials, sensitive headers, disabled Nex-only state, regex
flags, PAC/AutoDetect fallback extensions, independent Rule List intervals and other values that
original schema-v2 cannot directly express.

## 8. Current conclusions

1. The migration foundation is materially stronger than the historical progress narrative implied.
2. One authentic original-runtime-produced backup passes the background transaction oracle.
3. The same backup now passes complete packaged Chromium and Firefox migration journeys.
4. Corpus A is therefore green across both browser targets.
5. Broad compatibility still relies heavily on synthetic fixtures because Corpus B-D are incomplete.
6. Corpus B is a real external-input blocker; no owner daily-use backup is currently available.
7. Corpus C/D and field/failure evidence are repository-controlled work and should proceed now.
8. The parent `MIG-01` remains open; no release or owner-retest state changes follow from Corpus A.

## 9. Authorized next slice

Continue the contract at `MIG-01.5` — run Corpus B-D and close mapping, storage, activation and
browser gaps.

Immediate executable sequence:

1. inspect the pinned original-runtime fixture/provenance generation path;
2. generate provenance-bound Corpus C/D fixtures where the original runtime supports it;
3. extend the source-field preservation matrix from those real outputs;
4. drive the first Corpus C/D fixture through import -> activation -> real route/restart -> semantic
   export and fix the first real blocking layer;
5. keep Corpus B explicitly blocked until sanitized owner data exists rather than manufacturing it.

After Corpus B-D, continue to `MIG-01.6` negative/failure injection and `MIG-01.7` final semantic
round-trip/secret checks. Do not return to Popup or Options refinement before the migration parent
journey is closed.

## 10. Progress effect

The official/default Firefox slice adds strong evidence and closes Corpus A's second browser path,
but does not close the weighted migration parent capability.

Current audited state remains:

- product completion: `45.15%` -> reported `45%`;
- evidence confidence: `43%-50%`;
- release state: `NO-GO`;
- latest owner result: Firefox `FAIL`, 2026-08-02;
- owner retest, merge, release and candidate claims prohibited;
- active batch: `MIG-01`;
- next work: `MIG-01.5` Corpus B-D, beginning with repository-controlled Corpus C/D.
