# ZeroOmega Nex

A next-generation, cross-browser proxy profile manager focused on compatibility, deterministic policy compilation, and low-overhead browser-native execution.

## Development Status

Early architecture phase.

Goals:

- ZeroOmega profile compatibility
- Browser-native PAC execution path
- Modern TypeScript extension architecture
- Future Rust/WASM policy compiler
