name: CI

on:
  push:
    branches:
      - main
      - 'docs/**'
      - 'feat/**'
      - 'research/**'
  pull_request:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate

      - name: Register and format importer workspace once
        if: github.event_name == 'push' && github.ref_name == 'feat/m4-zeroomega-importer'
        shell: bash
        run: |
          pnpm install --no-frozen-lockfile
          pnpm exec prettier --write \
            package.json \
            packages/legacy-zeroomega/package.json \
            packages/legacy-zeroomega/tsconfig.json \
            packages/legacy-zeroomega/src/contracts.ts \
            packages/legacy-zeroomega/src/decode.ts \
            packages/legacy-zeroomega/src/decode.test.ts \
            packages/legacy-zeroomega/src/ids.ts \
            packages/legacy-zeroomega/src/index.ts \
            pnpm-lock.yaml
          if [[ -z "$(git status --porcelain)" ]]; then
            echo "Importer workspace already registered and formatted."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            package.json \
            packages/legacy-zeroomega \
            pnpm-lock.yaml
          git commit -m "chore(import): register and format importer workspace"
          git push origin HEAD:${GITHUB_REF_NAME}

      - name: Install locked dependencies
        shell: bash
        run: |
          if ! pnpm install --frozen-lockfile >pnpm-install.log 2>&1; then
            tail -n 80 pnpm-install.log
            exit 1
          fi
          tail -n 20 pnpm-install.log

      - name: Preserve dependency failure log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pnpm-install-log
          path: pnpm-install.log
          if-no-files-found: error

      - name: Verify, test, build, inspect, and package both browser targets
        shell: bash
        run: |
          if ! pnpm run verify >pnpm-verify.log 2>&1; then
            tail -n 160 pnpm-verify.log
            exit 1
          fi
          tail -n 60 pnpm-verify.log

      - name: Preserve verification failure log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pnpm-verify-log
          path: pnpm-verify.log
          if-no-files-found: error

      - name: Upload browser build archive
        uses: actions/upload-artifact@v4
        with:
          name: browser-builds
          path: browser-builds.tar.gz
          if-no-files-found: error
