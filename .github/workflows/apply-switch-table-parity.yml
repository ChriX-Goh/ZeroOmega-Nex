name: Apply Switch rule table parity

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/apply-switch-table-parity.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply Switch table parity slice
        run: |
          python <<'PY'
          from pathlib import Path
          from textwrap import dedent

          def replace_once(path: str, old: str, new: str) -> None:
              target = Path(path)
              source = target.read_text()
              count = source.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one match, found {count}: {old[:80]!r}')
              target.write_text(source.replace(old, new))

          switch_path = 'apps/extension/src/entrypoints/options/SwitchProfileEditor.svelte'
          old_conditions = dedent("""
            const conditionKinds: readonly { value: Condition['kind']; label: string }[] = [
              { value: 'host-wildcard', label: 'Host wildcard' },
              { value: 'url-wildcard', label: 'URL wildcard' },
              { value: 'host-regex', label: 'Host regular expression' },
              { value: 'url-regex', label: 'URL regular expression' },
              { value: 'keyword', label: 'URL keyword' },
              { value: 'bypass', label: 'Bypass pattern' },
              { value: 'ip', label: 'IP network' },
              { value: 'host-levels', label: 'Host levels' },
              { value: 'weekday', label: 'Weekday' },
              { value: 'time', label: 'Local time' },
              { value: 'true', label: 'Always' },
              { value: 'false', label: 'Never' },
            ];
          """).strip()
          new_conditions = dedent("""
            interface ConditionKindOption {
              readonly value: Condition['kind'];
              readonly label: string;
              readonly help: string;
            }

            interface ConditionGroup {
              readonly label: string;
              readonly options: readonly ConditionKindOption[];
            }

            const basicConditionGroups: readonly ConditionGroup[] = [
              {
                label: 'Basic conditions',
                options: [
                  {
                    value: 'host-wildcard',
                    label: 'Host wildcard',
                    help: 'Match a hostname pattern such as *.example.com.',
                  },
                  {
                    value: 'url-wildcard',
                    label: 'URL wildcard',
                    help: 'Match a complete URL wildcard pattern.',
                  },
                  {
                    value: 'url-regex',
                    label: 'URL regular expression',
                    help: 'Match a complete URL using a regular expression.',
                  },
                  {
                    value: 'false',
                    label: 'Never',
                    help: 'Keep a disabled placeholder rule without deleting it.',
                  },
                ],
              },
            ];

            const advancedConditionGroups: readonly ConditionGroup[] = [
              {
                label: 'Host',
                options: [
                  {
                    value: 'host-wildcard',
                    label: 'Host wildcard',
                    help: 'Match a hostname wildcard without path or port details.',
                  },
                  {
                    value: 'host-regex',
                    label: 'Host regular expression',
                    help: 'Match the hostname using a regular expression.',
                  },
                  {
                    value: 'host-levels',
                    label: 'Host levels',
                    help: 'Match hostnames whose label count falls inside a range.',
                  },
                  {
                    value: 'ip',
                    label: 'IP network',
                    help: 'Match an IPv4 or IPv6 network and prefix length.',
                  },
                  {
                    value: 'bypass',
                    label: 'Bypass pattern',
                    help: 'Match a browser bypass-list pattern.',
                  },
                ],
              },
              {
                label: 'URL',
                options: [
                  {
                    value: 'url-wildcard',
                    label: 'URL wildcard',
                    help: 'Match a complete URL wildcard pattern.',
                  },
                  {
                    value: 'url-regex',
                    label: 'URL regular expression',
                    help: 'Match a complete URL using a regular expression.',
                  },
                  {
                    value: 'keyword',
                    label: 'URL keyword',
                    help: 'Match an HTTP URL containing a keyword.',
                  },
                ],
              },
              {
                label: 'Special',
                options: [
                  {
                    value: 'weekday',
                    label: 'Weekday',
                    help: 'Match selected local weekdays.',
                  },
                  {
                    value: 'time',
                    label: 'Local time',
                    help: 'Match a local-time hour range.',
                  },
                  {
                    value: 'true',
                    label: 'Always',
                    help: 'Always match. Imported profiles may retain this explicit form.',
                  },
                  {
                    value: 'false',
                    label: 'Never',
                    help: 'Never match; useful as a retained placeholder.',
                  },
                ],
              },
            ];

            const basicConditionKinds = new Set(
              basicConditionGroups.flatMap((group) => group.options.map((option) => option.value)),
            );
          """).strip()
          replace_once(switch_path, old_conditions, new_conditions)

          old_weekdays = dedent("""
            const weekdays: readonly { value: Weekday; label: string }[] = [
              { value: 'mon', label: 'Mon' },
              { value: 'tue', label: 'Tue' },
              { value: 'wed', label: 'Wed' },
              { value: 'thu', label: 'Thu' },
              { value: 'fri', label: 'Fri' },
              { value: 'sat', label: 'Sat' },
              { value: 'sun', label: 'Sun' },
            ];
          """).strip()
          new_weekdays = dedent("""
            const weekdays: readonly { value: Weekday; label: string }[] = [
              { value: 'sun', label: 'Sun' },
              { value: 'mon', label: 'Mon' },
              { value: 'tue', label: 'Tue' },
              { value: 'wed', label: 'Wed' },
              { value: 'thu', label: 'Thu' },
              { value: 'fri', label: 'Fri' },
              { value: 'sat', label: 'Sat' },
            ];
          """).strip()
          replace_once(switch_path, old_weekdays, new_weekdays)

          old_state = dedent("""
            let profile: SwitchProfile | undefined;
            let routeProfiles: readonly UserProfile[] = [];
            $: profile = spec.profiles.find(
          """).strip()
          new_state = dedent("""
            let profile: SwitchProfile | undefined;
            let routeProfiles: readonly UserProfile[] = [];
            let showConditionHelp = false;
            let notesExpanded = false;
            let draggedRuleId: string | undefined;
            let useAdvancedConditions = false;
            let conditionGroups: readonly ConditionGroup[] = basicConditionGroups;
            let showNotes = false;
            let hasUrlConditions = false;
            $: profile = spec.profiles.find(
          """).strip()
          replace_once(switch_path, old_state, new_state)

          old_reactive = dedent("""
            $: routeProfiles = spec.profiles.filter((candidate) => candidate.id !== profileId);

            function valueFrom(event: Event): string {
          """).strip()
          new_reactive = dedent("""
            $: routeProfiles = spec.profiles.filter((candidate) => candidate.id !== profileId);
            $: useAdvancedConditions =
              spec.settings.interface.showAdvancedConditions ||
              (profile?.rules.some((rule) => !basicConditionKinds.has(rule.condition.kind)) ?? false);
            $: conditionGroups = useAdvancedConditions
              ? advancedConditionGroups
              : basicConditionGroups;
            $: showNotes = notesExpanded || (profile?.rules.some((rule) => Boolean(rule.note)) ?? false);
            $: hasUrlConditions =
              profile?.rules.some(
                (rule) => rule.condition.kind === 'url-wildcard' || rule.condition.kind === 'url-regex',
              ) ?? false;

            function valueFrom(event: Event): string {
          """).strip()
          replace_once(switch_path, old_reactive, new_reactive)

          old_add_rule = dedent("""
            async function addRule(kind: Condition['kind'], event: Event): Promise<void> {
              (event.currentTarget as HTMLSelectElement).value = '';
              try {
                const mutation = addSwitchRuleDraft(spec, profileId, idFactory, kind);
                await onReplaceDraft(mutation.draft);
              } catch {
                return;
              }
            }
          """).strip()
          new_add_rule = dedent("""
            async function addRule(): Promise<void> {
              try {
                const mutation = addSwitchRuleDraft(spec, profileId, idFactory, 'host-wildcard');
                await onReplaceDraft(mutation.draft);
              } catch {
                return;
              }
            }
          """).strip()
          replace_once(switch_path, old_add_rule, new_add_rule)

          old_weekday_update = dedent("""
            async function updateWeekdays(ruleId: string, event: Event): Promise<void> {
              const selected = [...(event.currentTarget as HTMLSelectElement).selectedOptions].map(
                (option) => option.value as Weekday,
              );
              await mutateRule(ruleId, (rule) => {
                if (rule.condition.kind === 'weekday') rule.condition.days = selected;
              });
            }
          """).strip()
          new_weekday_update = dedent("""
            async function toggleWeekday(ruleId: string, day: Weekday, selected: boolean): Promise<void> {
              await mutateRule(ruleId, (rule) => {
                if (rule.condition.kind !== 'weekday') return;
                const days = new Set(rule.condition.days);
                if (selected) days.add(day);
                else days.delete(day);
                rule.condition.days = weekdays
                  .map((entry) => entry.value)
                  .filter((entry) => days.has(entry));
              });
            }

            async function moveRuleTo(ruleId: string, targetIndex: number): Promise<void> {
              const draft = cloneProfileSpec(spec);
              const targetProfile = draft.profiles.find(
                (candidate): candidate is SwitchProfile =>
                  candidate.id === profileId && candidate.kind === 'switch',
              );
              if (!targetProfile) return;
              const sourceIndex = targetProfile.rules.findIndex((rule) => rule.id === ruleId);
              if (sourceIndex < 0 || sourceIndex === targetIndex) return;
              const [rule] = targetProfile.rules.splice(sourceIndex, 1);
              if (!rule) return;
              const boundedTarget = Math.max(0, Math.min(targetIndex, targetProfile.rules.length));
              targetProfile.rules.splice(boundedTarget, 0, rule);
              await onReplaceDraft(draft);
            }

            function startRuleDrag(ruleId: string, event: DragEvent): void {
              draggedRuleId = ruleId;
              event.dataTransfer?.setData('text/plain', ruleId);
              if (event.dataTransfer) event.dataTransfer.effectAllowed = 'move';
            }

            async function dropRule(targetIndex: number): Promise<void> {
              if (!draggedRuleId) return;
              const ruleId = draggedRuleId;
              draggedRuleId = undefined;
              await moveRuleTo(ruleId, targetIndex);
            }
          """).strip()
          replace_once(switch_path, old_weekday_update, new_weekday_update)

          source = Path(switch_path).read_text()
          marker = '\n{#if profile}'
          if source.count(marker) != 1:
              raise SystemExit(f'Switch markup marker mismatch: {source.count(marker)}')
          script = source[:source.index(marker)]
          markup = dedent(r'''
            {#if profile}
              {#if showConditionHelp}
                <section class="settings-section condition-help-section" data-switch-condition-help>
                  <div class="condition-help-header">
                    <h2>Condition help</h2>
                    <button
                      type="button"
                      class="close-help"
                      aria-label="Close condition help"
                      on:click={() => (showConditionHelp = false)}>×</button
                    >
                  </div>
                  <div class="condition-help-groups">
                    {#each conditionGroups as group, groupIndex (group.label)}
                      <details open={groupIndex === 0}>
                        <summary>{group.label}</summary>
                        <dl>
                          {#each group.options as option (option.value)}
                            <dt>{option.label}</dt>
                            <dd>{option.help}</dd>
                          {/each}
                        </dl>
                      </details>
                    {/each}
                  </div>
                </section>
              {/if}

              <section class="settings-section switch-rules-section">
                <div class="switch-rules-heading">
                  <div>
                    <h2>Switch rules</h2>
                    <p class="section-help">
                      Rules are evaluated from top to bottom. The first matching rule selects its result
                      profile.
                    </p>
                  </div>
                  <button
                    type="button"
                    class="help-button"
                    aria-expanded={showConditionHelp}
                    aria-controls="switch-condition-help"
                    on:click={() => (showConditionHelp = !showConditionHelp)}
                  >
                    ? Condition help
                  </button>
                </div>

                {#if hasUrlConditions}
                  <p class="url-condition-warning" role="alert">
                    Full-URL conditions depend on browser request information and may be limited for some
                    requests.
                  </p>
                {/if}

                <div class="table-scroller">
                  <table class="switch-rules-table" data-switch-rules-table>
                    <thead>
                      <tr>
                        <th scope="col">Sort</th>
                        <th scope="col">Condition type</th>
                        <th scope="col">Condition details</th>
                        <th scope="col">Result profile</th>
                        <th scope="col">Actions</th>
                        {#if showNotes}<th scope="col">Note</th>{/if}
                      </tr>
                    </thead>
                    <tbody>
                      {#each profile.rules as rule, index (rule.id)}
                        <tr
                          class:dragging={draggedRuleId === rule.id}
                          data-switch-rule-row={rule.id}
                          on:dragover|preventDefault
                          on:drop={() => dropRule(index)}
                        >
                          <td class="sort-cell">
                            <button
                              type="button"
                              class="drag-handle"
                              data-switch-drag-handle
                              title="Drag to reorder"
                              aria-label={`Drag rule ${index + 1} to reorder`}
                              aria-grabbed={draggedRuleId === rule.id}
                              draggable={!disabled}
                              disabled={disabled || profile.rules.length < 2}
                              on:dragstart={(event) => startRuleDrag(rule.id, event)}
                              on:dragend={() => (draggedRuleId = undefined)}
                            >
                              ↕
                            </button>
                            <label class="enabled-toggle" title="Rule enabled">
                              <input
                                type="checkbox"
                                checked={rule.enabled !== false}
                                {disabled}
                                aria-label={`Enable rule ${index + 1}`}
                                on:change={(event) => updateRuleEnabled(rule.id, checkedFrom(event))}
                              />
                            </label>
                            <div class="keyboard-order-actions">
                              <button
                                type="button"
                                aria-label={`Move rule ${index + 1} up`}
                                disabled={disabled || index === 0}
                                on:click={() => moveRule(rule.id, -1)}>↑</button
                              >
                              <button
                                type="button"
                                aria-label={`Move rule ${index + 1} down`}
                                disabled={disabled || index === profile.rules.length - 1}
                                on:click={() => moveRule(rule.id, 1)}>↓</button
                              >
                            </div>
                          </td>
                          <td>
                            <select
                              aria-label={`Rule ${index + 1} condition type`}
                              value={rule.condition.kind}
                              {disabled}
                              on:change={(event) =>
                                updateConditionKind(rule.id, valueFrom(event) as Condition['kind'])}
                            >
                              {#each conditionGroups as group (group.label)}
                                <optgroup label={group.label}>
                                  {#each group.options as option (option.value)}
                                    <option value={option.value}>{option.label}</option>
                                  {/each}
                                </optgroup>
                              {/each}
                            </select>
                          </td>
                          <td class="condition-details-cell">
                            {#if rule.condition.kind === 'true'}
                              <span>Always matches</span>
                            {:else if rule.condition.kind === 'false'}
                              <span>Never matches</span>
                            {:else if 'pattern' in rule.condition}
                              <div class="inline-details">
                                <input
                                  aria-label={`Rule ${index + 1} pattern`}
                                  value={conditionPattern(rule.condition)}
                                  {disabled}
                                  on:change={(event) =>
                                    updateConditionPattern(rule.id, valueFrom(event))}
                                />
                                {#if rule.condition.kind === 'host-regex' || rule.condition.kind === 'url-regex'}
                                  <input
                                    class="flags-input"
                                    aria-label={`Rule ${index + 1} regular-expression flags`}
                                    value={conditionFlags(rule.condition)}
                                    placeholder="flags"
                                    {disabled}
                                    on:change={(event) =>
                                      updateConditionFlags(rule.id, valueFrom(event))}
                                  />
                                {/if}
                              </div>
                            {:else if rule.condition.kind === 'ip'}
                              <div class="inline-details ip-details">
                                <input
                                  aria-label={`Rule ${index + 1} IP address`}
                                  value={conditionAddress(rule.condition)}
                                  placeholder="127.0.0.1"
                                  {disabled}
                                  on:change={(event) => updateIpAddress(rule.id, valueFrom(event))}
                                />
                                <span>/</span>
                                <input
                                  class="small-number"
                                  aria-label={`Rule ${index + 1} prefix length`}
                                  type="number"
                                  min="0"
                                  max="128"
                                  value={conditionNumber(rule.condition, 'prefixLength')}
                                  {disabled}
                                  on:change={(event) =>
                                    updateConditionNumber(rule.id, 'prefixLength', valueFrom(event))}
                                />
                              </div>
                            {:else if rule.condition.kind === 'host-levels'}
                              <div class="inline-details range-details">
                                <input
                                  class="small-number"
                                  aria-label={`Rule ${index + 1} minimum host levels`}
                                  type="number"
                                  min="1"
                                  max="99"
                                  value={conditionNumber(rule.condition, 'min')}
                                  {disabled}
                                  on:change={(event) =>
                                    updateConditionNumber(rule.id, 'min', valueFrom(event))}
                                />
                                <span>to</span>
                                <input
                                  class="small-number"
                                  aria-label={`Rule ${index + 1} maximum host levels`}
                                  type="number"
                                  min="1"
                                  max="99"
                                  value={conditionNumber(rule.condition, 'max')}
                                  {disabled}
                                  on:change={(event) =>
                                    updateConditionNumber(rule.id, 'max', valueFrom(event))}
                                />
                              </div>
                            {:else if rule.condition.kind === 'weekday'}
                              <div class="weekday-options" aria-label={`Rule ${index + 1} weekdays`}>
                                {#each weekdays as day (day.value)}
                                  <label class="weekday-option">
                                    <input
                                      type="checkbox"
                                      checked={conditionWeekdays(rule.condition).includes(day.value)}
                                      {disabled}
                                      on:change={(event) =>
                                        toggleWeekday(rule.id, day.value, checkedFrom(event))}
                                    />
                                    {day.label}
                                  </label>
                                {/each}
                              </div>
                            {:else if rule.condition.kind === 'time'}
                              <div class="inline-details range-details">
                                <input
                                  class="small-number"
                                  aria-label={`Rule ${index + 1} start hour`}
                                  type="number"
                                  min="0"
                                  max="23"
                                  value={conditionNumber(rule.condition, 'startHour')}
                                  {disabled}
                                  on:change={(event) =>
                                    updateConditionNumber(rule.id, 'startHour', valueFrom(event))}
                                />
                                <span>to</span>
                                <input
                                  class="small-number"
                                  aria-label={`Rule ${index + 1} end hour`}
                                  type="number"
                                  min="0"
                                  max="23"
                                  value={conditionNumber(rule.condition, 'endHour')}
                                  {disabled}
                                  on:change={(event) =>
                                    updateConditionNumber(rule.id, 'endHour', valueFrom(event))}
                                />
                              </div>
                            {/if}
                          </td>
                          <td>
                            <select
                              aria-label={`Rule ${index + 1} result profile`}
                              value={routeValue(rule.route)}
                              {disabled}
                              on:change={(event) => updateRuleRoute(rule.id, valueFrom(event))}
                            >
                              <option value="direct">Direct</option>
                              <option value="system">System Proxy</option>
                              {#each routeProfiles as target (target.id)}
                                <option value={`profile:${target.id}`}>{target.name}</option>
                              {/each}
                            </select>
                          </td>
                          <td>
                            <div class="row-actions">
                              <button
                                type="button"
                                title="Delete rule"
                                aria-label={`Delete rule ${index + 1}`}
                                {disabled}
                                on:click={() => deleteRule(rule.id)}>🗑</button
                              >
                              <button
                                type="button"
                                title="Clone rule"
                                aria-label={`Clone rule ${index + 1}`}
                                {disabled}
                                on:click={() => duplicateRule(rule.id)}>⧉</button
                              >
                              <button
                                type="button"
                                class:active-note={Boolean(rule.note)}
                                title="Add note"
                                aria-label={`Show note for rule ${index + 1}`}
                                {disabled}
                                on:click={() => (notesExpanded = true)}>✎</button
                              >
                            </div>
                          </td>
                          {#if showNotes}
                            <td>
                              <input
                                aria-label={`Rule ${index + 1} note`}
                                value={rule.note ?? ''}
                                placeholder="Optional note"
                                {disabled}
                                on:change={(event) => updateRuleNote(rule.id, valueFrom(event))}
                              />
                            </td>
                          {/if}
                        </tr>
                      {/each}
                      {#if profile.rules.length === 0}
                        <tr class="empty-rules-row">
                          <td></td>
                          <td colspan={showNotes ? 5 : 4}>
                            No conditions. Requests use the default profile below.
                          </td>
                        </tr>
                      {/if}
                      <tr class="add-condition-row">
                        <td></td>
                        <td colspan={showNotes ? 5 : 4}>
                          <button type="button" {disabled} on:click={addRule}>＋ Add condition</button>
                        </td>
                      </tr>
                      <tr class="default-route-row">
                        <td></td>
                        <th scope="row" colspan="2">Default profile</th>
                        <td>
                          <select
                            aria-label="Switch Profile default route"
                            value={routeValue(profile.defaultRoute)}
                            {disabled}
                            on:change={(event) => updateDefaultRoute(valueFrom(event))}
                          >
                            <option value="direct">Direct</option>
                            <option value="system">System Proxy</option>
                            {#each routeProfiles as target (target.id)}
                              <option value={`profile:${target.id}`}>{target.name}</option>
                            {/each}
                          </select>
                        </td>
                        <td></td>
                        {#if showNotes}<td></td>{/if}
                      </tr>
                    </tbody>
                  </table>
                </div>
              </section>
            {/if}

            <style>
              .condition-help-header,
              .switch-rules-heading {
                display: flex;
                align-items: flex-start;
                justify-content: space-between;
                gap: 1rem;
              }

              .condition-help-header h2,
              .switch-rules-heading h2 {
                margin-bottom: 0.35rem;
              }

              .close-help,
              .help-button {
                border: 1px solid var(--border-strong);
                background: var(--button-bg);
                border-radius: 3px;
                padding: 0.35rem 0.6rem;
              }

              .condition-help-groups {
                max-width: 960px;
              }

              .condition-help-groups details {
                border-top: 1px solid var(--border);
                padding: 0.55rem 0;
              }

              .condition-help-groups summary {
                font-weight: 600;
              }

              .condition-help-groups dl {
                display: grid;
                grid-template-columns: minmax(11rem, 0.35fr) minmax(16rem, 1fr);
                gap: 0.35rem 1rem;
                margin: 0.5rem 0 0;
              }

              .condition-help-groups dt {
                font-weight: 600;
              }

              .condition-help-groups dd {
                color: var(--muted);
                margin: 0;
              }

              .url-condition-warning {
                border: 1px solid var(--danger);
                color: var(--danger);
                max-width: 980px;
                padding: 0.65rem 0.8rem;
              }

              .table-scroller {
                overflow-x: auto;
              }

              .switch-rules-table {
                border-collapse: collapse;
                width: min(100%, 1180px);
                min-width: 900px;
              }

              .switch-rules-table th,
              .switch-rules-table td {
                border: 1px solid var(--border);
                padding: 0.38rem;
                text-align: left;
                vertical-align: middle;
              }

              .switch-rules-table thead th {
                background: var(--hover);
                font-size: 0.82rem;
                white-space: nowrap;
              }

              .switch-rules-table select,
              .switch-rules-table input {
                max-width: none;
                min-width: 0;
                width: 100%;
              }

              .switch-rules-table tbody tr.dragging {
                opacity: 0.55;
              }

              .sort-cell {
                width: 6.5rem;
                white-space: nowrap;
              }

              .drag-handle,
              .keyboard-order-actions button,
              .row-actions button,
              .add-condition-row button {
                border: 1px solid var(--border-strong);
                background: var(--button-bg);
                border-radius: 3px;
                min-height: 29px;
                padding: 0.25rem 0.45rem;
              }

              .drag-handle:not(:disabled) {
                cursor: grab;
              }

              .enabled-toggle {
                display: inline-flex;
                margin: 0 0.25rem;
              }

              .enabled-toggle input,
              .weekday-option input {
                width: auto;
              }

              .keyboard-order-actions,
              .row-actions,
              .inline-details,
              .weekday-options {
                display: flex;
                align-items: center;
                gap: 0.3rem;
              }

              .keyboard-order-actions {
                display: inline-flex;
              }

              .row-actions {
                flex-wrap: nowrap;
              }

              .row-actions button.active-note {
                border-color: var(--accent);
                color: var(--accent-strong);
              }

              .condition-details-cell {
                min-width: 15rem;
              }

              .flags-input {
                max-width: 5rem !important;
              }

              .small-number {
                max-width: 4.5rem !important;
              }

              .ip-details input:first-child {
                min-width: 9rem;
              }

              .weekday-options {
                flex-wrap: wrap;
              }

              .weekday-option {
                display: inline-flex;
                align-items: center;
                gap: 0.2rem;
                margin: 0;
                white-space: nowrap;
              }

              .empty-rules-row td,
              .add-condition-row td {
                color: var(--muted);
              }

              .default-route-row th,
              .default-route-row td {
                background: var(--hover);
              }

              @media (max-width: 760px) {
                .condition-help-header,
                .switch-rules-heading {
                  align-items: stretch;
                  flex-direction: column;
                }

                .condition-help-groups dl {
                  grid-template-columns: 1fr;
                }
              }
            </style>
          ''').strip()
          Path(switch_path).write_text(script + '\n\n' + markup + '\n')

          operations_path = 'packages/profile-workflow/src/switch-operations.ts'
          old_push = dedent("""
              profile.rules.push({
                id: ruleId,
                condition: createDefaultSwitchCondition(conditionKind),
                route: { kind: 'direct' },
              });
          """).strip()
          new_push = dedent("""
              const rule: SwitchRule = {
                id: ruleId,
                condition: createDefaultSwitchCondition(conditionKind),
                route: structuredClone(profile.defaultRoute),
              };
              if (draft.settings.interface.addConditionsToBottom) profile.rules.push(rule);
              else profile.rules.unshift(rule);
          """).strip()
          replace_once(operations_path, old_push, new_push)

          operations_test_path = 'packages/profile-workflow/src/switch-operations.test.ts'
          test_marker = dedent("""
              it('moves and deletes rules while preserving first-match order', () => {
          """).strip()
          insertion = dedent("""
              it('uses the default result profile and respects top-or-bottom insertion settings', () => {
                const ids = deterministicIds();
                const created = createSwitchProfileDraft(workflowFixture(), ids);
                const profile = switchProfile(created.draft, created.profileId);
                profile.defaultRoute = { kind: 'system' };

                const first = addSwitchRuleDraft(created.draft, created.profileId, ids, 'host-wildcard');
                expect(switchProfile(first.draft, created.profileId).rules[0]?.route).toEqual({
                  kind: 'system',
                });

                first.draft.settings.interface.addConditionsToBottom = false;
                const second = addSwitchRuleDraft(first.draft, created.profileId, ids, 'url-wildcard');
                expect(switchProfile(second.draft, created.profileId).rules.map((rule) => rule.id)).toEqual([
                  second.ruleId,
                  first.ruleId,
                ]);
              });

          """)
          target = Path(operations_test_path)
          source = target.read_text()
          if source.count(test_marker) != 1:
              raise SystemExit(f'Switch operation test marker mismatch: {source.count(test_marker)}')
          target.write_text(source.replace(test_marker, insertion + test_marker))

          component_test_path = 'apps/extension/src/component-rendering.component.spec.ts'
          replace_once(
              component_test_path,
              '  createDefaultProfileSpec,\n',
              '  addSwitchRuleDraft,\n  createDefaultProfileSpec,\n',
          )
          old_test = dedent("""
              it('renders an empty ordered Switch Profile editor with disabled controls', () => {
                const mutation = createSwitchProfileDraft(baseSpec(), idFactory());
                const { body } = render(SwitchProfileEditor, {
                  props: {
                    spec: mutation.draft,
                    profileId: mutation.profileId,
                    disabled: true,
                    idFactory: idFactory(),
                    onReplaceDraft: replaceDraft,
                  },
                });

                expect(body).toContain('Default route');
                expect(body).toContain('Rules are evaluated from top to bottom');
                expect(body).toContain('No rules. This profile always uses its default route.');
                expect(body).toContain('aria-label="Add Switch Profile rule"');
                expect(body).toContain('disabled');
              });
          """).strip()
          new_test = dedent("""
              it('renders the original compact Switch Profile rule table and grouped conditions', () => {
                const ids = idFactory();
                const created = createSwitchProfileDraft(baseSpec(), ids);
                const mutation = addSwitchRuleDraft(created.draft, created.profileId, ids, 'host-wildcard');
                const { body } = render(SwitchProfileEditor, {
                  props: {
                    spec: mutation.draft,
                    profileId: created.profileId,
                    disabled: true,
                    idFactory: ids,
                    onReplaceDraft: replaceDraft,
                  },
                });

                expect(body).toContain('data-switch-rules-table');
                expect(body).toContain('data-switch-rule-row');
                expect(body).toContain('data-switch-drag-handle');
                expect(body).toContain('Condition type');
                expect(body).toContain('Condition details');
                expect(body).toContain('Result profile');
                expect(body).toContain('Add condition');
                expect(body).toContain('Default profile');
                expect(body).toContain('<optgroup label="Basic conditions">');
                expect(body).not.toContain('Ordered Switch Profile rules');
              });
          """).strip()
          replace_once(component_test_path, old_test, new_test)

          guard_path = 'scripts/validate-ui-compatibility.mjs'
          guard = Path(guard_path).read_text()
          guard = guard.replace(
              "const fixedProfilePath = 'apps/extension/src/entrypoints/options/FixedProfileEditor.svelte';\n",
              "const fixedProfilePath = 'apps/extension/src/entrypoints/options/FixedProfileEditor.svelte';\nconst switchProfilePath = 'apps/extension/src/entrypoints/options/SwitchProfileEditor.svelte';\n",
              1,
          )
          guard = guard.replace('  fixedProfile,\n  optionsHtml,', '  fixedProfile,\n  switchProfile,\n  optionsHtml,', 1)
          guard = guard.replace(
              "  readFile(fixedProfilePath, 'utf8'),\n  readFile(optionsHtmlPath, 'utf8'),",
              "  readFile(fixedProfilePath, 'utf8'),\n  readFile(switchProfilePath, 'utf8'),\n  readFile(optionsHtmlPath, 'utf8'),",
              1,
          )
          guard_marker = dedent("""
              [
                profileOperations.includes('proxyByScheme: {}') &&
          """).strip()
          switch_guard = dedent("""
              [
                switchProfile.includes('data-switch-rules-table') &&
                  switchProfile.includes('data-switch-rule-row') &&
                  switchProfile.includes('data-switch-drag-handle') &&
                  switchProfile.includes('Condition help') &&
                  switchProfile.includes('<optgroup label={group.label}>') &&
                  switchProfile.includes('Default profile') &&
                  !switchProfile.includes('Ordered Switch Profile rules'),
                'Switch Profile must use the original compact rule table, grouped condition help, drag handle, and table-bottom default row.',
              ],
          """)
          if guard.count(guard_marker) != 1:
              raise SystemExit(f'Switch guard marker mismatch: {guard.count(guard_marker)}')
          Path(guard_path).write_text(guard.replace(guard_marker, switch_guard + guard_marker))

          audit_path = 'docs/UI_AUDIT_MATRIX.md'
          lines = Path(audit_path).read_text().splitlines()
          replacements = {
              '| D-01 |': '| D-01 | 条件帮助区       | `profile_switch.jade`  | 基础/高级分组，可展开关闭       | MUST_MATCH | PARTIAL    | MISSING | 已恢复可展开关闭的分组帮助区；文本仍待 locale 化                         | 接入原版 locale         |',
              '| D-02 |': '| D-02 | 规则表格         | 同上                   | 排序/类型/细节/结果/动作/备注列 | MUST_MATCH | DONE       | PARTIAL | 已恢复单一紧凑表格及原版列结构；组件测试与永久守卫覆盖                    | 浏览器视觉巡查          |',
              '| D-03 |': '| D-03 | 拖动排序         | 同上                   | drag handle 排序                | MUST_MATCH | PARTIAL    | N/A     | 已有原生 drag handle 与键盘 Up/Down 后备；尚缺浏览器拖放 E2E             | 加 Chromium 拖放测试    |',
              '| D-04 |': '| D-04 | 条件类型下拉     | 同上                   | 原版类型和分组                  | MUST_MATCH | PARTIAL    | MISSING | 已恢复基础/Host/URL/Special optgroup，并兼容现有扩展条件                  | locale 与原版命名巡查   |',
              '| D-05 |': '| D-05 | 条件专属字段     | 同上                   | 不同条件用专属控件              | MUST_MATCH | PARTIAL    | MISSING | 表格内恢复 pattern、regex flags、IP、层级、星期、时段专属控件             | 验证与警告逐项对齐      |',
              '| D-06 |': '| D-06 | 结果情景模式列   | 同上                   | 每规则 profile selector         | MUST_MATCH | DONE       | PARTIAL | 每行 result profile selector 已回到表格列                                | locale 与浏览器巡查     |',
              '| D-07 |': '| D-07 | 删除规则         | 同上                   | 行内删除按钮                    | MUST_MATCH | DONE       | PARTIAL | 行内删除已回到 Actions 列                                                 | 补确认对话框巡查        |',
              '| D-08 |': '| D-08 | 复制规则         | 同上                   | 行内 clone                      | MUST_MATCH | DONE       | PARTIAL | 行内 clone 已回到 Actions 列                                              | 浏览器巡查              |',
              '| D-09 |': '| D-09 | 备注列           | 同上                   | 可按需显示/添加备注             | MUST_MATCH | DONE       | PARTIAL | 点击备注动作展开可选 Note 列；已有备注自动显示                           | locale 与交互巡查       |',
              '| D-10 |': '| D-10 | 添加条件位置     | 同上、UI setting       | 顶部/底部由设置决定             | MUST_MATCH | DONE       | PARTIAL | addConditionsToBottom 现决定 unshift/push，新增单元测试                   | 浏览器设置联动测试      |',
              '| D-11 |': '| D-11 | 默认情景模式行   | 同上                   | 表格尾部独立默认行              | MUST_MATCH | DONE       | PARTIAL | Default profile 已恢复为表格尾部独立行；新规则继承该路由                  | locale 与视觉巡查       |',
          }
          seen = set()
          for index, line in enumerate(lines):
              for prefix, replacement in replacements.items():
                  if line.startswith(prefix):
                      lines[index] = replacement
                      seen.add(prefix)
          if seen != set(replacements):
              raise SystemExit(f'Missing Switch audit rows: {set(replacements) - seen}')
          update_row = '| 2026-07-26 | 清除 URL、规则正文和请求头的 Nex 假默认值；H-09/H-10/H-11 以代码、单测和永久守卫验证 |'
          if update_row in lines:
              lines.insert(
                  lines.index(update_row) + 1,
                  '| 2026-07-26 | Switch 首切片恢复紧凑规则表、条件分组帮助、拖序/键盘排序、备注列、默认路由行与新增位置语义 |',
              )
          Path(audit_path).write_text('\n'.join(lines) + '\n')

          knowledge_path = 'docs/ORIGINAL_KNOWLEDGE_GRAPH.md'
          knowledge = Path(knowledge_path).read_text()
          condition_anchor = dedent("""
            ### 条件语义

            - 条件类型、帮助、限制提示和字段应由原版条件模型驱动。
            - URL 条件有完整 URL 能力限制警告。
            - 新增条件插入顶部或底部由界面设置决定。
          """).strip()
          condition_replacement = condition_anchor + dedent("""

            ### SwitchProfile 当前实现状态（2026-07-26）

            - 已恢复单一紧凑规则表，列结构为排序、条件类型、条件细节、结果情景模式、动作和可选备注。
            - 条件类型按基础或 Host/URL/Special 分组；现有高级条件会自动展开高级分组，避免导入数据失去可编辑项。
            - 已提供原生拖动 handle 与键盘 Up/Down 后备；新增条件按 `addConditionsToBottom` 插入顶部或底部，并继承表格尾部默认路由。
            - 图形/源码双向编辑、附属 RuleList、原版 locale 文本和浏览器拖放 E2E 尚未完成，因此 Switch 整体仍是 `PARTIAL`。
          """)
          if knowledge.count(condition_anchor) != 1:
              raise SystemExit(f'Switch knowledge anchor mismatch: {knowledge.count(condition_anchor)}')
          knowledge = knowledge.replace(condition_anchor, condition_replacement)
          revision = '| 2026-07-26 | 清除 PAC/Rule List 模式切换和新增请求头时写入配置的 Nex 假默认值；新增永久防回归守卫                                         | 原版 `profile_pac.jade`、`profile_rule_list.jade` 的空输入与 placeholder 语义 |'
          if knowledge.count(revision) != 1:
              raise SystemExit(f'Knowledge revision row mismatch: {knowledge.count(revision)}')
          knowledge = knowledge.replace(
              revision,
              revision + '\n| 2026-07-26 | Switch 首切片恢复紧凑规则表、基础/高级分组帮助、排序、备注、默认路由行及新增位置语义                                         | 原版 `profile_switch.jade`、`switch_profile.coffee`                            |',
          )
          Path(knowledge_path).write_text(knowledge)
          PY
      - name: Format changed files
        run: |
          pnpm exec prettier --write \
            apps/extension/src/entrypoints/options/SwitchProfileEditor.svelte \
            apps/extension/src/component-rendering.component.spec.ts \
            packages/profile-workflow/src/switch-operations.ts \
            packages/profile-workflow/src/switch-operations.test.ts \
            scripts/validate-ui-compatibility.mjs \
            docs/ORIGINAL_KNOWLEDGE_GRAPH.md \
            docs/UI_AUDIT_MATRIX.md
      - name: Verify repository
        run: pnpm verify
      - name: Remove generated outputs and one-time workflow
        run: |
          rm -rf browser-builds browser-builds.tar.gz
          git clean -fdX
          rm -f .github/workflows/apply-switch-table-parity.yml
          git diff --check
      - name: Commit verified Switch slice
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'feat: restore original Switch rule table'
          git push origin HEAD:feat/m8-profile-workflow
