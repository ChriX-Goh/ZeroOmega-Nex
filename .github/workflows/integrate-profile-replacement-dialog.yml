# integration revision 3
name: Integrate profile replacement dialog

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/integrate-profile-replacement-dialog.yml

permissions:
  contents: write

concurrency:
  group: integrate-profile-replacement-dialog-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply profile replacement dialog slice
        env:
          PROFILE_REPLACEMENT_RUN_ID: ${{ github.run_id }}
        run: |
          set -o pipefail
          {
            set -x
            python .github/patches/fix-profile-replacement-patch.py
            python .github/patches/apply-profile-replacement-dialog.py
            python .github/patches/fix-generated-profile-replacement-types.py
          } 2>&1 | tee profile-replacement-apply.log
      - name: Format product slice
        run: pnpm format
      - name: Verify repository
        run: |
          set -o pipefail
          pnpm verify 2>&1 | tee profile-replacement-verify.log
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Build Chromium E2E candidate
        env:
          ZEROOMEGA_RULE_SOURCE_E2E: '1'
        run: pnpm build:chromium
      - name: Run Chromium regression suite
        run: |
          set -o pipefail
          pnpm test:e2e:chromium 2>&1 | tee profile-replacement-e2e.log
      - name: Upload integration evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: profile-replacement-integration-${{ github.run_id }}
          path: profile-replacement-*.log
          if-no-files-found: warn
          retention-days: 7
      - name: Commit verified product slice
        run: |
          git checkout HEAD -- .github/workflows/integrate-profile-replacement-dialog.yml
          rm -f \
            .github/patches/fix-profile-replacement-patch.py \
            .github/patches/apply-profile-replacement-dialog.py \
            .github/patches/fix-generated-profile-replacement-types.py \
            profile-replacement-apply.log \
            profile-replacement-verify.log \
            profile-replacement-e2e.log
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat: restore general profile replacement dialog"
          git fetch origin feat/m8-profile-workflow
          git rebase origin/feat/m8-profile-workflow
          git push origin HEAD:feat/m8-profile-workflow
