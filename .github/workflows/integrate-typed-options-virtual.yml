name: Integrate typed normal Options and Virtual

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/integrate-typed-options-virtual.yml
      - .github/patches/*typed-options-virtual*.py

permissions:
  contents: write

concurrency:
  group: integrate-typed-options-virtual-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply typed normal Options and Virtual slice
        run: |
          set -o pipefail
          {
            set -x
            python .github/patches/write-typed-options-virtual.py
            python .github/patches/fix-typed-options-virtual-export-patch.py
            python .github/patches/patch-typed-options-virtual.py
            python .github/patches/patch-typed-options-virtual-tests-docs.py
          } 2>&1 | tee typed-options-virtual-apply.log
      - name: Format product slice
        run: |
          set -o pipefail
          pnpm format 2>&1 | tee typed-options-virtual-format.log
      - name: Generate locale inventory
        run: |
          set -o pipefail
          pnpm locale:inventory 2>&1 | tee typed-options-virtual-inventory.log
          pnpm exec prettier --write docs/LOCALE_INVENTORY.json
      - name: Verify repository
        run: |
          set -o pipefail
          pnpm verify 2>&1 | tee typed-options-virtual-verify.log
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Build Chromium E2E candidate
        env:
          ZEROOMEGA_RULE_SOURCE_E2E: '1'
        run: pnpm build:chromium
      - name: Run Chromium normal Options and Virtual regression
        run: |
          set -o pipefail
          pnpm test:e2e:chromium 2>&1 | tee typed-options-virtual-chromium.log
      - name: Upload integration evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: typed-options-virtual-${{ github.run_id }}
          path: typed-options-virtual-*.log
          if-no-files-found: warn
          retention-days: 7
      - name: Commit verified product slice
        run: |
          git checkout HEAD -- .github/workflows/integrate-typed-options-virtual.yml
          rm -f \
            .github/patches/write-typed-options-virtual.py \
            .github/patches/fix-typed-options-virtual-export-patch.py \
            .github/patches/patch-typed-options-virtual.py \
            .github/patches/patch-typed-options-virtual-tests-docs.py \
            typed-options-virtual-apply.log \
            typed-options-virtual-format.log \
            typed-options-virtual-inventory.log \
            typed-options-virtual-verify.log \
            typed-options-virtual-chromium.log
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat: add typed normal Options and Virtual"
          git fetch origin feat/m8-profile-workflow
          git rebase origin/feat/m8-profile-workflow
          git push origin HEAD:feat/m8-profile-workflow
