name: Export Formatted Renderer Implementation

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/export-formatted-renderer-implementation.yml
      - apps/extension/src/lib/original-toolbar-action-adapter.ts
      - apps/extension/src/lib/original-toolbar-action-adapter.test.ts
      - apps/extension/src/lib/original-toolbar-icon-renderer.ts
      - apps/extension/src/lib/original-toolbar-icon-renderer.test.ts

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Format targets in workspace
        run: >-
          pnpm exec prettier --write
          apps/extension/src/lib/original-toolbar-action-adapter.ts
          apps/extension/src/lib/original-toolbar-action-adapter.test.ts
          apps/extension/src/lib/original-toolbar-icon-renderer.ts
          apps/extension/src/lib/original-toolbar-icon-renderer.test.ts

      - name: Upload formatted sources
        uses: actions/upload-artifact@v4
        with:
          name: formatted-renderer-implementation
          path: |
            apps/extension/src/lib/original-toolbar-action-adapter.ts
            apps/extension/src/lib/original-toolbar-action-adapter.test.ts
            apps/extension/src/lib/original-toolbar-icon-renderer.ts
            apps/extension/src/lib/original-toolbar-icon-renderer.test.ts
          if-no-files-found: error
