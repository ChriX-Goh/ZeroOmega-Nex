name: Sync toolbar runtime status

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/sync-toolbar-runtime-status.yml
      - scripts/sync-toolbar-runtime-status.py

permissions:
  contents: write

jobs:
  sync:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
          pnpm install --frozen-lockfile
      - name: Sync and verify
        run: |
          python scripts/sync-toolbar-runtime-status.py
          pnpm exec prettier --write \
            docs/ACTIVE_PARITY_AUDIT_INDEX.md \
            docs/MILESTONE_8_STATUS.md \
            docs/MILESTONE_8_VERIFICATION_HEAD.md
          pnpm verify
      - name: Commit
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            docs/ACTIVE_PARITY_AUDIT_INDEX.md \
            docs/MILESTONE_8_STATUS.md \
            docs/MILESTONE_8_VERIFICATION_HEAD.md
          git commit -m 'docs: sync verified toolbar runtime status'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
