# revision 2
name: Integrate Popup temporary rules

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/integrate-popup-temporary-rules.yml

permissions:
  contents: write

concurrency:
  group: integrate-popup-temporary-rules-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Apply reviewed temporary-rule slice
        id: apply_slice
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            python .github/patches/apply-popup-temporary-rules-core.py
            python .github/patches/apply-popup-temporary-rules-background.py
            python .github/patches/apply-popup-temporary-rules-runtime.py
            python .github/patches/apply-popup-temporary-rules-coordinator.py
            python .github/patches/apply-popup-temporary-rules-ui.py
            python .github/patches/apply-popup-temporary-rules-evidence.py
          } 2>&1 | tee /tmp/popup-temporary-apply.log
      - name: Upload patch diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-temporary-apply
          path: /tmp/popup-temporary-apply.log
          if-no-files-found: warn
      - name: Enforce patch result
        if: steps.apply_slice.outcome != 'success'
        run: exit 1
      - name: Install locked dependencies
        id: install
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm install --frozen-lockfile 2>&1 | tee /tmp/popup-temporary-install.log
      - name: Upload install diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-temporary-install
          path: /tmp/popup-temporary-install.log
          if-no-files-found: warn
      - name: Enforce install result
        if: steps.install.outcome != 'success'
        run: exit 1
      - name: Format and inspect product diff
        id: format
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            pnpm exec prettier --write \
              packages/profile-workflow/src/popup-temporary-rules.ts \
              packages/profile-workflow/src/popup-temporary-rules.test.ts \
              packages/profile-workflow/src/index.ts \
              apps/extension/src/lib/browser-proxy-runtime.ts \
              apps/extension/src/lib/profile-workflow-activation.ts \
              apps/extension/src/lib/session-snapshot-repository.ts \
              apps/extension/src/lib/session-snapshot-repository.test.ts \
              apps/extension/src/lib/popup-temporary-rule-client.ts \
              apps/extension/src/lib/popup-temporary-rule-runtime.ts \
              apps/extension/src/lib/popup-temporary-rule-runtime.test.ts \
              apps/extension/src/entrypoints/background.ts \
              apps/extension/src/entrypoints/popup/App.svelte \
              apps/extension/src/entrypoints/popup/style.css \
              apps/extension/src/entrypoints/temp-rules/index.html \
              apps/extension/src/entrypoints/temp-rules/main.ts \
              apps/extension/src/entrypoints/temp-rules/App.svelte \
              apps/extension/src/entrypoints/temp-rules/style.css \
              scripts/e2e-chromium.mjs \
              scripts/validate-ui-compatibility.mjs \
              docs/ORIGINAL_KNOWLEDGE_GRAPH.md \
              docs/UI_AUDIT_MATRIX.md \
              docs/MILESTONE_8_STATUS.md
            git diff --check
          } 2>&1 | tee /tmp/popup-temporary-format.log
      - name: Upload format diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-temporary-format
          path: /tmp/popup-temporary-format.log
          if-no-files-found: warn
      - name: Enforce format result
        if: steps.format.outcome != 'success'
        run: exit 1
      - name: Verify complete repository
        id: verify
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm verify 2>&1 | tee /tmp/popup-temporary-verify.log
      - name: Upload verification diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-temporary-verification
          path: /tmp/popup-temporary-verify.log
          if-no-files-found: warn
      - name: Enforce verification result
        if: steps.verify.outcome != 'success'
        run: exit 1
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Run Chromium temporary-rule E2E
        id: chromium
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          ZEROOMEGA_RULE_SOURCE_E2E=1 pnpm build:chromium
          pnpm test:e2e:chromium 2>&1 | tee /tmp/popup-temporary-chromium.log
      - name: Upload Chromium diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-temporary-chromium
          path: /tmp/popup-temporary-chromium.log
          if-no-files-found: warn
      - name: Enforce Chromium result
        if: steps.chromium.outcome != 'success'
        run: exit 1
      - name: Clean temporary integration files
        run: |
          rm -rf browser-builds browser-builds.tar.gz
          git clean -fdX
          rm -rf .github/patches
          rm -f .github/workflows/integrate-popup-temporary-rules.yml
          git diff --check
      - name: Commit verified product slice
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin feat/m8-profile-workflow
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/feat/m8-profile-workflow)"
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --stat
          git commit -m 'feat: add Popup temporary rules'
          git push origin HEAD:feat/m8-profile-workflow
