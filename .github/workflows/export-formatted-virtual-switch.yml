name: Export Formatted Virtual Switch

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/export-formatted-virtual-switch.yml
      - apps/extension/src/lib/original-observable-virtual-switch.ts
      - apps/extension/src/lib/original-toolbar-virtual-switch.test.ts

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Format targets in workspace
        run: >-
          pnpm exec prettier --write
          apps/extension/src/lib/original-observable-virtual-switch.ts
          apps/extension/src/lib/original-toolbar-virtual-switch.test.ts

      - name: Upload formatted sources
        uses: actions/upload-artifact@v4
        with:
          name: formatted-virtual-switch
          path: |
            apps/extension/src/lib/original-observable-virtual-switch.ts
            apps/extension/src/lib/original-toolbar-virtual-switch.test.ts
          if-no-files-found: error
