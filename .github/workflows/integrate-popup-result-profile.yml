# revision 4
name: Integrate Popup result profiles

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/integrate-popup-result-profile.yml

permissions:
  contents: write

concurrency:
  group: integrate-popup-result-profile-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Apply reviewed Popup result slice
        id: apply_slice
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            python .github/patches/fix-popup-result-patch.py
            python .github/patches/apply-popup-result-profile.py
          } 2>&1 | tee /tmp/popup-result-apply.log
      - name: Upload patch diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-result-apply
          path: /tmp/popup-result-apply.log
          if-no-files-found: warn
      - name: Enforce patch result
        if: steps.apply_slice.outcome != 'success'
        run: exit 1
      - name: Install locked dependencies
        id: install
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm install --frozen-lockfile 2>&1 | tee /tmp/popup-result-install.log
      - name: Upload install diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-result-install
          path: /tmp/popup-result-install.log
          if-no-files-found: warn
      - name: Enforce install result
        if: steps.install.outcome != 'success'
        run: exit 1
      - name: Format and inspect product diff
        id: format
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          {
            pnpm exec prettier --write \
              packages/profile-workflow/src/popup-condition.ts \
              packages/profile-workflow/src/popup-condition.test.ts \
              packages/profile-workflow/src/popup-result-command.test.ts \
              packages/profile-workflow/src/commands.ts \
              packages/profile-workflow/src/index.ts \
              apps/extension/src/entrypoints/popup/App.svelte \
              apps/extension/src/entrypoints/popup/style.css \
              scripts/e2e-chromium.mjs \
              scripts/validate-ui-compatibility.mjs \
              docs/ORIGINAL_KNOWLEDGE_GRAPH.md \
              docs/UI_AUDIT_MATRIX.md \
              docs/MILESTONE_8_STATUS.md
            git diff --check
          } 2>&1 | tee /tmp/popup-result-format.log
      - name: Upload format diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-result-format
          path: /tmp/popup-result-format.log
          if-no-files-found: warn
      - name: Enforce format result
        if: steps.format.outcome != 'success'
        run: exit 1
      - name: Verify complete repository
        id: verify
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm verify 2>&1 | tee /tmp/popup-result-verify.log
      - name: Upload verification diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-result-verification
          path: /tmp/popup-result-verify.log
          if-no-files-found: warn
      - name: Enforce verification result
        if: steps.verify.outcome != 'success'
        run: exit 1
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Run Chromium Popup result E2E
        id: chromium
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          ZEROOMEGA_RULE_SOURCE_E2E=1 pnpm build:chromium
          pnpm test:e2e:chromium 2>&1 | tee /tmp/popup-result-chromium.log
      - name: Upload Chromium diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: popup-result-chromium
          path: /tmp/popup-result-chromium.log
          if-no-files-found: warn
      - name: Enforce Chromium result
        if: steps.chromium.outcome != 'success'
        run: exit 1
      - name: Clean temporary integration files
        run: |
          rm -rf browser-builds browser-builds.tar.gz
          git clean -fdX
          rm -f .github/patches/apply-popup-result-profile.py
          rm -f .github/patches/fix-popup-result-patch.py
          rmdir .github/patches 2>/dev/null || true
          rm -f .github/workflows/integrate-popup-result-profile.yml
          git diff --check
      - name: Commit verified product slice
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin feat/m8-profile-workflow
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/feat/m8-profile-workflow)"
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --stat
          git commit -m 'feat: add Popup result profile controls'
          git push origin HEAD:feat/m8-profile-workflow
