name: Integrate typed General and Interface

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/integrate-typed-general-interface.yml
      - .github/patches/*typed-general-interface*.py

permissions:
  contents: write

concurrency:
  group: integrate-typed-general-interface-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply typed General and Interface slice
        run: |
          set -o pipefail
          {
            set -x
            python .github/patches/fix-typed-general-interface-patch.py
            python .github/patches/apply-typed-general-interface.py
            python .github/patches/fix-typed-general-interface-guards.py
          } 2>&1 | tee typed-general-interface-apply.log
      - name: Format product slice
        run: |
          set -o pipefail
          pnpm format 2>&1 | tee typed-general-interface-format.log
      - name: Generate locale inventory
        run: |
          set -o pipefail
          pnpm locale:inventory 2>&1 | tee typed-general-interface-inventory.log
          pnpm exec prettier --write docs/LOCALE_INVENTORY.json
      - name: Verify repository
        run: |
          set -o pipefail
          pnpm verify 2>&1 | tee typed-general-interface-verify.log
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium
      - name: Build Chromium E2E candidate
        env:
          ZEROOMEGA_RULE_SOURCE_E2E: '1'
        run: pnpm build:chromium
      - name: Run Chromium General and Interface regression
        run: |
          set -o pipefail
          pnpm test:e2e:chromium 2>&1 | tee typed-general-interface-chromium.log
      - name: Upload integration evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: typed-general-interface-${{ github.run_id }}
          path: typed-general-interface-*.log
          if-no-files-found: warn
          retention-days: 7
      - name: Commit verified product slice
        run: |
          git checkout HEAD -- .github/workflows/integrate-typed-general-interface.yml
          rm -f \
            .github/patches/fix-typed-general-interface-patch.py \
            .github/patches/fix-typed-general-interface-guards.py \
            .github/patches/apply-typed-general-interface.py \
            typed-general-interface-apply.log \
            typed-general-interface-format.log \
            typed-general-interface-inventory.log \
            typed-general-interface-verify.log \
            typed-general-interface-chromium.log
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat: add typed General and Interface options"
          git fetch origin feat/m8-profile-workflow
          git rebase origin/feat/m8-profile-workflow
          git push origin HEAD:feat/m8-profile-workflow
