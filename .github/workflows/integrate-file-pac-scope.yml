name: Integrate file PAC scope

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/integrate-file-pac-scope.yml
      - .github/patches/apply-file-pac-scope.py

permissions:
  contents: write

concurrency:
  group: integrate-file-pac-scope-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: github.head_ref == 'feat/m8-profile-workflow' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/m8-profile-workflow
          fetch-depth: 0
          show-progress: false

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply file PAC scope decision
        run: |
          set -o pipefail
          python .github/patches/apply-file-pac-scope.py 2>&1 | tee file-pac-scope-apply.log

      - name: Format product slice
        run: |
          set -o pipefail
          pnpm format 2>&1 | tee file-pac-scope-format.log

      - name: Generate classified locale inventory
        run: |
          set -o pipefail
          pnpm locale:inventory 2>&1 | tee file-pac-scope-inventory.log
          pnpm exec prettier --write docs/LOCALE_INVENTORY.json

      - name: Verify repository
        run: |
          set -o pipefail
          pnpm verify 2>&1 | tee file-pac-scope-verify.log

      - name: Upload integration evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: file-pac-scope-${{ github.run_id }}
          path: file-pac-scope-*.log
          if-no-files-found: warn
          retention-days: 7

      - name: Commit verified product slice
        run: |
          git checkout HEAD -- .github/workflows/integrate-file-pac-scope.yml
          rm -f \
            .github/patches/apply-file-pac-scope.py \
            file-pac-scope-apply.log \
            file-pac-scope-format.log \
            file-pac-scope-inventory.log \
            file-pac-scope-verify.log
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "docs: decide browser-only file PAC scope"
          git fetch origin feat/m8-profile-workflow
          git rebase origin/feat/m8-profile-workflow
          git push origin HEAD:feat/m8-profile-workflow
